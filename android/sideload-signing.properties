# Committed sideload keystore (android/sideload.keystore), same key as
# TepMex/CloudAgenticCoding so sideloaded APK updates install over previous builds.
# Not for Play Store distribution.
storeFile=sideload.keystore
storePassword=ankidroidllm
keyAlias=ankidroidllm
keyPassword=ankidroidllm
